# 新评分表下一步行动计划 📋

## 🎯 目标

完成新评分表的完整集成，使其可以在生产环境中使用。

---

## 📅 行动计划

### 阶段1: 前端页面集成（预计1-2天）

#### 1.1 更新自评页面路由
**文件**: `frontend/src/router/index.ts`

**任务**:
- [ ] 添加新评分表页面路由
- [ ] 配置路由守卫（权限检查）
- [ ] 测试路由跳转

**示例代码**:
```typescript
{
  path: '/self-evaluation/new',
  name: 'NewSelfEvaluation',
  component: () => import('@/views/NewSelfEvaluation.vue'),
  meta: { requiresAuth: true, role: 'teaching_office' }
}
```

#### 1.2 创建新评分表页面
**文件**: `frontend/src/views/NewSelfEvaluation.vue`

**任务**:
- [ ] 创建页面组件
- [ ] 引入NewSelfEvaluationForm组件
- [ ] 实现保存、预览、提交功能
- [ ] 添加加载状态和错误处理

**示例代码**:
```vue
<template>
  <div class="new-self-evaluation-page">
    <PageHeader title="填写考评表" />
    
    <NewSelfEvaluationForm
      :teaching-office-id="teachingOfficeId"
      :evaluation-year="currentYear"
      :evaluation-id="evaluationId"
      @save="handleSave"
      @preview="handlePreview"
      @submit="handleSubmit"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import NewSelfEvaluationForm from '@/components/NewSelfEvaluationForm.vue'
import PageHeader from '@/components/PageHeader.vue'
import { saveSelfEvaluation, submitSelfEvaluation } from '@/api/self-evaluation'

const route = useRoute()
const router = useRouter()

const teachingOfficeId = ref('')
const evaluationId = ref('')
const currentYear = ref(new Date().getFullYear())

onMounted(async () => {
  // 获取教研室ID
  teachingOfficeId.value = route.params.id as string
  
  // 如果有evaluationId，加载已保存的数据
  if (route.query.evaluationId) {
    evaluationId.value = route.query.evaluationId as string
    // TODO: 加载评估数据
  }
})

const handleSave = async (data: any) => {
  try {
    const result = await saveSelfEvaluation(data)
    evaluationId.value = result.id
    ElMessage.success('保存成功')
  } catch (error) {
    ElMessage.error('保存失败')
  }
}

const handlePreview = (data: any) => {
  // TODO: 显示预览对话框
  console.log('预览数据:', data)
}

const handleSubmit = async (id: string) => {
  try {
    await submitSelfEvaluation(id)
    ElMessage.success('提交成功')
    router.push('/self-evaluation/list')
  } catch (error) {
    ElMessage.error('提交失败')
  }
}
</script>
```

#### 1.3 更新API调用
**文件**: `frontend/src/api/self-evaluation.ts`

**任务**:
- [ ] 创建保存自评表的API函数
- [ ] 创建加载自评表的API函数
- [ ] 创建提交自评表的API函数
- [ ] 添加类型定义

**示例代码**:
```typescript
import { client } from './client'

export interface SelfEvaluationData {
  teachingOfficeId: string
  evaluationYear: number
  regularTeaching: any
  highlights: any
  negativeList: any
}

export const saveSelfEvaluation = async (data: SelfEvaluationData) => {
  const response = await client.post('/api/v1/self-evaluations', data)
  return response.data
}

export const loadSelfEvaluation = async (id: string) => {
  const response = await client.get(`/api/v1/self-evaluations/${id}`)
  return response.data
}

export const submitSelfEvaluation = async (id: string) => {
  const response = await client.post(`/api/v1/self-evaluations/${id}/submit`)
  return response.data
}
```

---

### 阶段2: 后端API更新（预计1天）

#### 2.1 更新自评表端点
**文件**: `backend/app/api/v1/endpoints/self_evaluation.py`

**任务**:
- [ ] 确保POST端点支持新的数据结构
- [ ] 确保GET端点返回新的数据结构
- [ ] 添加数据验证
- [ ] 更新响应模型

**检查点**:
```python
@router.post("/", response_model=SelfEvaluationResponse)
async def create_self_evaluation(
    evaluation: SelfEvaluationCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    # 验证content结构
    content = evaluation.content
    
    # 检查是否包含新结构的必需字段
    if 'regularTeaching' not in content:
        raise HTTPException(status_code=400, detail="缺少regularTeaching字段")
    
    if 'highlights' not in content:
        raise HTTPException(status_code=400, detail="缺少highlights字段")
    
    if 'negativeList' not in content:
        raise HTTPException(status_code=400, detail="缺少negativeList字段")
    
    # 创建自评表
    db_evaluation = SelfEvaluation(
        teaching_office_id=evaluation.teaching_office_id,
        evaluation_year=evaluation.evaluation_year,
        content=content,
        status="draft"
    )
    
    db.add(db_evaluation)
    db.commit()
    db.refresh(db_evaluation)
    
    return db_evaluation
```

#### 2.2 更新Schema定义
**文件**: `backend/app/schemas/self_evaluation.py`

**任务**:
- [ ] 添加新结构的类型定义
- [ ] 更新验证规则
- [ ] 添加示例数据

**示例代码**:
```python
from pydantic import BaseModel, Field
from typing import List, Dict, Any

class RegularTeachingItem(BaseModel):
    content: str = Field(..., min_length=10, max_length=1000)
    selfScore: float = Field(..., ge=0, le=10)
    maxScore: int = Field(default=10)

class RegularTeaching(BaseModel):
    teachingProcessManagement: RegularTeachingItem
    teachingQualityManagement: RegularTeachingItem
    courseAssessment: RegularTeachingItem
    educationResearch: RegularTeachingItem
    courseConstruction: RegularTeachingItem
    teacherTeamBuilding: RegularTeachingItem
    researchAndExchange: RegularTeachingItem
    archiveManagement: RegularTeachingItem

class HighlightItem(BaseModel):
    name: str
    level: str
    score: float

class HighlightCategory(BaseModel):
    items: List[HighlightItem]
    totalScore: float

class Highlights(BaseModel):
    teachingReformProjects: HighlightCategory
    teachingHonors: HighlightCategory
    teachingCompetitions: HighlightCategory
    innovationCompetitions: HighlightCategory

class NegativeItem(BaseModel):
    count: int = Field(default=0, ge=0)
    deduction: float = Field(default=0, ge=0)

class NegativeList(BaseModel):
    ethicsViolations: NegativeItem
    teachingAccidents: NegativeItem
    ideologyIssues: NegativeItem
    workloadIncomplete: Dict[str, Any]  # percentage and deduction

class SelfEvaluationContent(BaseModel):
    regularTeaching: RegularTeaching
    highlights: Highlights
    negativeList: NegativeList

class SelfEvaluationCreate(BaseModel):
    teaching_office_id: str
    evaluation_year: int
    content: SelfEvaluationContent
```

---

### 阶段3: 附件上传集成（预计2天）

#### 3.1 为亮点项目添加附件上传
**文件**: `frontend/src/components/NewSelfEvaluationForm.vue`

**任务**:
- [ ] 在每个亮点项目表格中添加附件上传列
- [ ] 实现附件上传功能
- [ ] 显示已上传的附件列表
- [ ] 支持附件删除

**示例代码**:
```vue
<el-table-column label="附件" width="200">
  <template #default="{ row, $index }">
    <el-upload
      :action="`/api/v1/attachments/upload`"
      :data="{ 
        evaluationId: evaluationId,
        indicator: 'teaching_reform_projects',
        projectIndex: $index
      }"
      :on-success="(response) => handleUploadSuccess(response, row)"
      :show-file-list="false"
    >
      <el-button size="small" type="primary">上传附件</el-button>
    </el-upload>
    
    <div v-if="row.attachments && row.attachments.length > 0">
      <el-tag
        v-for="(file, index) in row.attachments"
        :key="index"
        closable
        @close="handleRemoveAttachment(row, index)"
        style="margin: 2px;"
      >
        {{ file.name }}
      </el-tag>
    </div>
  </template>
</el-table-column>
```

#### 3.2 更新数据结构
**任务**:
- [ ] 在每个项目对象中添加attachments字段
- [ ] 保存时包含附件信息
- [ ] 加载时恢复附件信息

---

### 阶段4: 预览功能（预计1天）

#### 4.1 创建预览对话框
**文件**: `frontend/src/components/SelfEvaluationPreview.vue`

**任务**:
- [ ] 创建预览对话框组件
- [ ] 美化预览样式（类似打印格式）
- [ ] 显示所有评分信息
- [ ] 添加打印按钮

**示例代码**:
```vue
<template>
  <el-dialog
    v-model="visible"
    title="考评表预览"
    width="80%"
    :close-on-click-modal="false"
  >
    <div class="preview-content" ref="printArea">
      <h1>教研室工作考核评分表</h1>
      <p>被考核教研室: {{ data.teachingOfficeName }}</p>
      <p>评估年度: {{ data.evaluationYear }}</p>
      
      <!-- 常规教学工作 -->
      <h2>一、常规教学工作</h2>
      <table class="preview-table">
        <thead>
          <tr>
            <th>考核指标</th>
            <th>内容</th>
            <th>自评分</th>
          </tr>
        </thead>
        <tbody>
          <tr v-for="(item, key) in data.regularTeaching" :key="key">
            <td>{{ getIndicatorName(key) }}</td>
            <td>{{ item.content }}</td>
            <td>{{ item.selfScore }}</td>
          </tr>
        </tbody>
      </table>
      
      <!-- 特色与亮点项目 -->
      <h2>二、特色与亮点项目</h2>
      <!-- ... -->
      
      <!-- 负面清单 -->
      <h2>三、负面清单</h2>
      <!-- ... -->
      
      <!-- 总分 -->
      <div class="total-score">
        <h3>最终得分: {{ calculateFinalScore() }} 分</h3>
      </div>
    </div>
    
    <template #footer>
      <el-button @click="visible = false">关闭</el-button>
      <el-button type="primary" @click="handlePrint">打印</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const visible = ref(false)
const printArea = ref()

const handlePrint = () => {
  window.print()
}
</script>

<style scoped>
.preview-content {
  padding: 20px;
}

.preview-table {
  width: 100%;
  border-collapse: collapse;
  margin: 20px 0;
}

.preview-table th,
.preview-table td {
  border: 1px solid #ddd;
  padding: 8px;
  text-align: left;
}

.preview-table th {
  background-color: #f2f2f2;
}

@media print {
  .el-dialog__header,
  .el-dialog__footer {
    display: none;
  }
}
</style>
```

---

### 阶段5: 测试（预计2天）

#### 5.1 单元测试
**任务**:
- [ ] 前端组件测试
- [ ] 后端服务测试
- [ ] API端点测试

#### 5.2 集成测试
**任务**:
- [ ] 完整流程测试（创建→保存→提交→AI评分）
- [ ] 附件上传测试
- [ ] 异常检测测试
- [ ] 边界条件测试

#### 5.3 用户验收测试
**任务**:
- [ ] 邀请用户测试
- [ ] 收集反馈
- [ ] 修复问题
- [ ] 优化体验

---

### 阶段6: 部署（预计1天）

#### 6.1 测试环境部署
**任务**:
- [ ] 部署后端更新
- [ ] 部署前端更新
- [ ] 验证功能正常
- [ ] 性能测试

#### 6.2 生产环境部署
**任务**:
- [ ] 备份数据库
- [ ] 部署后端
- [ ] 部署前端
- [ ] 监控日志
- [ ] 准备回滚方案

---

## 🎯 优先级

### P0 - 必须完成（核心功能）
1. ✅ 前端表单组件（已完成）
2. ✅ 后端AI评分服务（已完成）
3. ⏳ 前端页面集成
4. ⏳ 后端API更新
5. ⏳ 基本测试

### P1 - 重要功能
1. ⏳ 附件上传集成
2. ⏳ 预览功能
3. ⏳ 完整测试

### P2 - 可选功能
1. ⏳ 数据迁移（如果需要）
2. ⏳ 导出PDF
3. ⏳ 批量操作

---

## 📊 时间估算

| 阶段 | 预计时间 | 依赖 |
|------|---------|------|
| 阶段1: 前端页面集成 | 1-2天 | 无 |
| 阶段2: 后端API更新 | 1天 | 无 |
| 阶段3: 附件上传集成 | 2天 | 阶段1 |
| 阶段4: 预览功能 | 1天 | 阶段1 |
| 阶段5: 测试 | 2天 | 阶段1-4 |
| 阶段6: 部署 | 1天 | 阶段5 |
| **总计** | **7-8天** | - |

---

## ⚠️ 风险和注意事项

### 技术风险

1. **数据兼容性**
   - 风险: 旧数据无法使用新表单
   - 缓解: 添加版本检测，提供数据迁移工具

2. **API性能**
   - 风险: AI评分时间过长
   - 缓解: 添加超时处理，显示进度提示

3. **附件上传**
   - 风险: 大文件上传失败
   - 缓解: 使用分片上传，添加重试机制

### 业务风险

1. **用户培训**
   - 风险: 用户不熟悉新表单
   - 缓解: 提供用户手册，添加操作提示

2. **数据丢失**
   - 风险: 保存失败导致数据丢失
   - 缓解: 自动保存草稿，添加本地缓存

3. **评分争议**
   - 风险: AI评分与人工评分差异大
   - 缓解: 保留人工复核流程，记录评分理由

---

## 📝 检查清单

### 开发前检查
- [ ] 确认需求理解正确
- [ ] 确认技术方案可行
- [ ] 确认时间安排合理
- [ ] 确认资源充足

### 开发中检查
- [ ] 代码符合规范
- [ ] 添加必要的注释
- [ ] 编写单元测试
- [ ] 更新文档

### 开发后检查
- [ ] 功能测试通过
- [ ] 性能测试通过
- [ ] 安全测试通过
- [ ] 用户验收通过

### 部署前检查
- [ ] 备份数据库
- [ ] 准备回滚方案
- [ ] 通知相关人员
- [ ] 准备监控工具

### 部署后检查
- [ ] 验证功能正常
- [ ] 检查错误日志
- [ ] 监控性能指标
- [ ] 收集用户反馈

---

## 🎯 成功标准

### 功能标准
- ✅ 用户可以填写新评分表
- ✅ 系统可以保存和加载评分表
- ✅ 系统可以提交评分表并触发AI评分
- ✅ 系统可以检测异常并转人工复核
- ✅ 系统可以分类附件

### 性能标准
- ✅ 表单加载时间 < 2秒
- ✅ 保存响应时间 < 1秒
- ✅ AI评分时间 < 30秒
- ✅ 页面无明显卡顿

### 质量标准
- ✅ 无严重Bug
- ✅ 代码覆盖率 > 80%
- ✅ 用户满意度 > 90%
- ✅ 系统稳定性 > 99%

---

## 📞 联系方式

如有问题，请联系：

- **技术支持**: [技术团队邮箱]
- **产品经理**: [产品经理邮箱]
- **项目经理**: [项目经理邮箱]

---

## 📚 参考资料

- [新评分表实施方案](./新评分表实施方案.md)
- [新评分表完成说明](./新评分表完成说明.md)
- [新评分表后端集成完成说明](./新评分表后端集成完成说明.md)
- [新评分表集成总结](./新评分表集成总结.md)
- [教研室工作考核评分表 (2)](./教研室工作考核评分表%20(2).md)

---

**创建时间**: 2026-02-13  
**版本**: v1.0  
**状态**: 待执行 ⏳  
**预计完成时间**: 2026-02-21
