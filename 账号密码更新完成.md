# 测试账号密码更新完成

## 更新时间
2026-02-13

## ✅ 已完成的工作

### 1. 数据库更新
- 删除了旧的测试账号 (`test_teaching_office`, `test_eval_team`)
- 创建了新的简化测试账号

### 2. 新测试账号

#### 教研室端
- **用户名**: `123`
- **密码**: `123`
- **角色**: teaching_office
- **用户ID**: a1b2c3d4-e5f6-4a5b-8c9d-0e1f2a3b4c5d

#### 管理端 - 考评小组
- **用户名**: `admin`
- **密码**: `123`
- **角色**: evaluation_team
- **用户ID**: b2c3d4e5-f6a7-4b5c-9d0e-1f2a3b4c5d6e

### 3. 登录测试验证
✅ 教研室端登录测试通过
✅ 管理端登录测试通过

### 4. 文档更新
已更新以下文档：
- ✅ `项目介绍文档.md` - 添加了测试账号信息
- ✅ `QUICK_START.md` - 添加了登录测试步骤
- ✅ `测试账号更新说明.md` - 新建详细说明文档

## 🔍 验证方法

### 方法1: 数据库查询
```bash
mysql -u root -proot teaching_office_evaluation -e "SELECT username, name, role FROM users WHERE username IN ('123', 'admin');"
```

预期输出：
```
username    name                    role
123         Teaching Office User    teaching_office
admin       Evaluation Team User    evaluation_team
```

### 方法2: API测试

#### 测试教研室端登录
```powershell
Invoke-RestMethod -Uri "http://localhost:8000/api/auth/login" -Method POST -ContentType "application/json" -Body '{"username":"123","password":"123","role":"teaching_office"}'
```

#### 测试管理端登录
```powershell
Invoke-RestMethod -Uri "http://localhost:8000/api/auth/login" -Method POST -ContentType "application/json" -Body '{"username":"admin","password":"123","role":"evaluation_team"}'
```

### 方法3: 前端测试

#### 教研室端
1. 访问: http://localhost:3000
2. 点击"教研室端"卡片
3. 输入用户名: `123`，密码: `123`
4. 点击登录
5. ✅ 应该成功跳转到教研室主页

#### 管理端
1. 访问: http://localhost:3000
2. 点击"管理端"卡片
3. 输入用户名: `admin`，密码: `123`
4. 选择角色: 评教小组
5. 点击登录
6. ✅ 应该成功跳转到管理端主页

## 📝 技术细节

### 密码哈希
两个账号使用相同的bcrypt哈希（密码都是 `123`）：
```
$2b$12$fsGArgXk2SbWTHHcH6sLZefVnvyPSFmn5Sy7AqG1DeR1gNjqRGxiO
```

### UUID格式
MySQL中UUID存储为CHAR(36)字符串格式，例如：
```
a1b2c3d4-e5f6-4a5b-8c9d-0e1f2a3b4c5d
```

### SQL脚本
创建了 `update_test_accounts.sql` 文件，包含：
- 删除旧账号的SQL
- 插入新账号的SQL
- 验证查询的SQL

## ⚠️ 注意事项

1. **仅用于测试**: 这些账号仅用于开发和测试环境
2. **生产环境**: 生产环境必须使用更安全的密码
3. **旧账号失效**: 所有文档中提到的旧账号已失效
4. **角色选择**: 管理端登录时需要手动选择角色

## 📚 相关文档

- `测试账号更新说明.md` - 详细的账号说明
- `项目介绍文档.md` - 项目介绍和快速开始
- `QUICK_START.md` - 快速启动指南
- `update_test_accounts.sql` - SQL更新脚本

## 🎉 总结

测试账号已成功更新为简单易记的格式：
- 教研室端: `123` / `123`
- 管理端: `admin` / `123`

所有测试均已通过，系统可以正常使用！
