# 教研室数据管理平台 - 宝塔面板部署指南

## 📋 系统信息

### 端口配置
- **前端端口**: 3000 (开发) / 80 (生产，通过Nginx)
- **后端端口**: 8000
- **数据库端口**: 5432 (PostgreSQL) 或 3306 (MySQL)
- **服务器IP**: 101.33.211.98

### 数据库信息
- **当前开发环境**: SQLite (`backend/test.db`)
- **生产环境推荐**: PostgreSQL 或 MySQL
- **数据库名**: `teaching_office_evaluation`
- **用户名**: `teaching_office_user`

---

## 🚀 宝塔面板部署步骤

### 步骤 1: 安装宝塔面板

如果还没有安装宝塔面板，SSH登录服务器后执行：

```bash
# Ubuntu/Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh

# CentOS
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh
```

安装完成后，记录：
- 宝塔面板地址: http://101.33.211.98:8888
- 用户名和密码

### 步骤 2: 在宝塔面板安装必要软件

登录宝塔面板后，在"软件商店"安装：

1. **Nginx** (1.20+)
2. **Python项目管理器** (或手动安装Python 3.11)
3. **PostgreSQL** (15.0+) 或 **MySQL** (8.0+)
4. **PM2管理器** (用于管理Node.js进程)

### 步骤 3: 创建数据库

#### 方案A: 使用PostgreSQL（推荐）

1. 在宝塔面板点击 **数据库** → **PostgreSQL**
2. 点击 **添加数据库**
3. 填写信息：
   - 数据库名: `teaching_office_evaluation`
   - 用户名: `teaching_office_user`
   - 密码: 设置一个强密码（记录下来）
4. 点击 **提交**

#### 方案B: 使用MySQL

1. 在宝塔面板点击 **数据库** → **MySQL**
2. 点击 **添加数据库**
3. 填写信息：
   - 数据库名: `teaching_office_evaluation`
   - 用户名: `teaching_office_user`
   - 密码: 设置一个强密码（记录下来）
   - 访问权限: 本地服务器
4. 点击 **提交**

### 步骤 4: 上传项目文件

#### 方法1: 使用宝塔面板上传

1. 在宝塔面板点击 **文件**
2. 进入 `/www/wwwroot/` 目录
3. 创建新文件夹: `teaching-office-platform`
4. 点击 **上传** 按钮
5. 上传项目压缩包（排除 node_modules 和 dist）

#### 方法2: 使用命令行上传（推荐）

在本地电脑执行：

```bash
# 压缩项目
tar -czf teaching-office-platform.tar.gz \
  --exclude='node_modules' \
  --exclude='frontend/dist' \
  --exclude='backend/__pycache__' \
  --exclude='*.pyc' \
  .

# 上传到服务器
scp teaching-office-platform.tar.gz root@101.33.211.98:/www/wwwroot/

# SSH登录服务器
ssh root@101.33.211.98

# 解压
cd /www/wwwroot
tar -xzf teaching-office-platform.tar.gz
mv teaching-office-platform teaching-office-platform-temp
mkdir teaching-office-platform
mv teaching-office-platform-temp/* teaching-office-platform/
rm -rf teaching-office-platform-temp
```

### 步骤 5: 配置后端环境

#### 5.1 安装Python依赖

SSH登录服务器：

```bash
cd /www/wwwroot/teaching-office-platform/backend

# 安装依赖
pip3 install -r requirements.txt
```

#### 5.2 配置环境变量

```bash
cd /www/wwwroot/teaching-office-platform/backend

# 复制生产环境配置
cp .env.production .env

# 编辑配置文件
nano .env
```

**PostgreSQL配置**:
```env
# 数据库配置
USE_SQLITE=false
POSTGRES_SERVER=localhost
POSTGRES_USER=teaching_office_user
POSTGRES_PASSWORD=你在宝塔设置的数据库密码
POSTGRES_DB=teaching_office_evaluation
POSTGRES_PORT=5432

# 安全配置
SECRET_KEY=你的随机密钥  # 使用 openssl rand -hex 32 生成

# CORS配置
BACKEND_CORS_ORIGINS=["http://101.33.211.98","http://101.33.211.98:3000","http://101.33.211.98:8000"]

# DeepSeek API（可选）
DEEPSEEK_API_KEY=你的API密钥
```

**MySQL配置**（如果使用MySQL）:
```env
# 数据库配置
USE_SQLITE=false
DATABASE_URL=mysql+pymysql://teaching_office_user:你的密码@localhost:3306/teaching_office_evaluation

# 其他配置同上
```

生成随机密钥：
```bash
openssl rand -hex 32
```

#### 5.3 运行数据库迁移

```bash
cd /www/wwwroot/teaching-office-platform/backend

# 运行迁移
alembic upgrade head
```

### 步骤 6: 配置前端

#### 6.1 安装Node.js依赖

```bash
cd /www/wwwroot/teaching-office-platform/frontend

# 安装依赖
npm install

# 构建生产版本
npm run build
```

构建完成后，静态文件在 `frontend/dist` 目录。

### 步骤 7: 在宝塔面板配置网站

#### 7.1 添加网站

1. 在宝塔面板点击 **网站** → **添加站点**
2. 填写信息：
   - 域名: `101.33.211.98` (或你的域名)
   - 根目录: `/www/wwwroot/teaching-office-platform/frontend/dist`
   - PHP版本: 纯静态
3. 点击 **提交**

#### 7.2 配置Nginx反向代理

1. 点击网站的 **设置** 按钮
2. 点击 **配置文件**
3. 找到 `server` 块，修改为：

```nginx
server {
    listen 80;
    server_name 101.33.211.98;
    
    # 前端静态文件
    root /www/wwwroot/teaching-office-platform/frontend/dist;
    index index.html;
    
    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # API文档
    location /docs {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 文件上传大小限制
    client_max_body_size 100M;
    
    access_log /www/wwwlogs/teaching-office-access.log;
    error_log /www/wwwlogs/teaching-office-error.log;
}
```

4. 点击 **保存**
5. 重载Nginx配置

### 步骤 8: 配置后端服务（使用Supervisor）

#### 8.1 在宝塔面板添加Supervisor守护进程

1. 在宝塔面板点击 **软件商店**
2. 搜索并安装 **Supervisor管理器**
3. 安装完成后，点击 **设置**
4. 点击 **添加守护进程**
5. 填写信息：

```
名称: teaching-office-backend
启动用户: root
运行目录: /www/wwwroot/teaching-office-platform/backend
启动命令: /usr/bin/python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
进程数量: 1
```

6. 点击 **确定**
7. 点击 **启动** 按钮

### 步骤 9: 配置防火墙和安全组

#### 9.1 宝塔面板防火墙

1. 在宝塔面板点击 **安全**
2. 放行以下端口：
   - 80 (HTTP)
   - 443 (HTTPS)
   - 8000 (后端API，可选)
   - 8888 (宝塔面板)

#### 9.2 腾讯云安全组

1. 登录腾讯云控制台
2. 进入 **云服务器** → **安全组**
3. 编辑安全组规则，添加：
   - 入站规则: TCP 80 (0.0.0.0/0)
   - 入站规则: TCP 443 (0.0.0.0/0)
   - 入站规则: TCP 8000 (0.0.0.0/0)

### 步骤 10: 验证部署

```bash
# 检查后端服务
curl http://127.0.0.1:8000/api/health

# 检查前端
curl http://101.33.211.98

# 在浏览器访问
http://101.33.211.98
```

---

## 📦 数据库迁移方案

### 方案1: 从SQLite迁移到PostgreSQL

#### 1.1 导出SQLite数据

```bash
cd /www/wwwroot/teaching-office-platform/backend

# 安装sqlite3-to-postgresql工具
pip3 install sqlite3-to-postgresql

# 导出数据
sqlite3-to-postgresql \
  --sqlite-file test.db \
  --postgres-dsn "postgresql://teaching_office_user:你的密码@localhost:5432/teaching_office_evaluation"
```

#### 1.2 手动迁移（如果工具不可用）

```bash
# 1. 导出SQLite数据为SQL
sqlite3 test.db .dump > data.sql

# 2. 编辑data.sql，修改语法兼容PostgreSQL
# 主要修改：
# - 删除 BEGIN TRANSACTION 和 COMMIT
# - 修改自增字段语法
# - 修改日期时间格式

# 3. 导入到PostgreSQL
psql -U teaching_office_user -d teaching_office_evaluation -f data.sql
```

### 方案2: 从SQLite迁移到MySQL

```bash
# 1. 安装转换工具
pip3 install mysql-connector-python

# 2. 使用Python脚本迁移
python3 << 'EOF'
import sqlite3
import mysql.connector

# 连接SQLite
sqlite_conn = sqlite3.connect('test.db')
sqlite_cursor = sqlite_conn.cursor()

# 连接MySQL
mysql_conn = mysql.connector.connect(
    host='localhost',
    user='teaching_office_user',
    password='你的密码',
    database='teaching_office_evaluation'
)
mysql_cursor = mysql_conn.cursor()

# 获取所有表
sqlite_cursor.execute("SELECT name FROM sqlite_master WHERE type='table';")
tables = sqlite_cursor.fetchall()

# 迁移每个表
for table in tables:
    table_name = table[0]
    print(f"迁移表: {table_name}")
    
    # 读取数据
    sqlite_cursor.execute(f"SELECT * FROM {table_name}")
    rows = sqlite_cursor.fetchall()
    
    # 插入数据（需要根据实际表结构调整）
    # ...

sqlite_conn.close()
mysql_conn.close()
EOF
```

### 方案3: 使用Alembic重新初始化（推荐）

如果开发环境数据不重要，最简单的方法：

```bash
cd /www/wwwroot/teaching-office-platform/backend

# 1. 确保.env配置正确（USE_SQLITE=false）
# 2. 运行迁移创建表结构
alembic upgrade head

# 3. 如果需要初始数据，运行初始化脚本
python3 -c "from app.db.init_db import init_db; init_db()"
```

---

## 🔧 常用管理命令

### 通过宝塔面板管理

1. **查看后端日志**:
   - 宝塔面板 → Supervisor管理器 → teaching-office-backend → 日志

2. **重启后端服务**:
   - 宝塔面板 → Supervisor管理器 → teaching-office-backend → 重启

3. **查看Nginx日志**:
   - 宝塔面板 → 网站 → 你的网站 → 日志

4. **数据库管理**:
   - 宝塔面板 → 数据库 → PostgreSQL/MySQL → phpPgAdmin/phpMyAdmin

### 通过SSH命令行管理

```bash
# 重启后端
supervisorctl restart teaching-office-backend

# 查看后端状态
supervisorctl status teaching-office-backend

# 查看后端日志
tail -f /tmp/teaching-office-backend-stdout.log

# 重启Nginx
/etc/init.d/nginx restart

# 查看Nginx日志
tail -f /www/wwwlogs/teaching-office-error.log
```

---

## 🔄 更新部署

当代码更新后：

```bash
# 1. 上传新代码
scp -r frontend/src root@101.33.211.98:/www/wwwroot/teaching-office-platform/frontend/
scp -r backend/app root@101.33.211.98:/www/wwwroot/teaching-office-platform/backend/

# 2. SSH登录服务器
ssh root@101.33.211.98

# 3. 更新前端
cd /www/wwwroot/teaching-office-platform/frontend
npm install
npm run build

# 4. 更新后端
cd /www/wwwroot/teaching-office-platform/backend
pip3 install -r requirements.txt
alembic upgrade head

# 5. 重启服务
supervisorctl restart teaching-office-backend

# 6. 重载Nginx
/etc/init.d/nginx reload
```

---

## 🐛 常见问题

### 问题1: 后端无法启动

**解决方法**:
```bash
# 查看详细错误
cd /www/wwwroot/teaching-office-platform/backend
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 检查端口占用
netstat -tlnp | grep 8000

# 检查Python版本
python3 --version  # 需要3.11+
```

### 问题2: 数据库连接失败

**解决方法**:
```bash
# 检查数据库服务
systemctl status postgresql  # 或 mysql

# 测试数据库连接
psql -U teaching_office_user -d teaching_office_evaluation  # PostgreSQL
mysql -u teaching_office_user -p teaching_office_evaluation  # MySQL

# 检查.env配置
cat /www/wwwroot/teaching-office-platform/backend/.env
```

### 问题3: 前端页面空白

**解决方法**:
```bash
# 检查dist目录
ls -la /www/wwwroot/teaching-office-platform/frontend/dist

# 重新构建
cd /www/wwwroot/teaching-office-platform/frontend
npm run build

# 检查Nginx配置
nginx -t

# 查看Nginx错误日志
tail -f /www/wwwlogs/teaching-office-error.log
```

### 问题4: API请求跨域错误

**解决方法**:
1. 检查 `backend/app/core/config.py` 的 CORS 配置
2. 确保包含: `"http://101.33.211.98"`
3. 重启后端服务

---

## 📊 性能优化建议

### 1. 启用Gzip压缩

在Nginx配置中添加：
```nginx
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
```

### 2. 配置静态资源缓存

已在上面的Nginx配置中包含

### 3. 数据库优化

- 定期备份数据库
- 创建必要的索引
- 定期清理日志

### 4. 使用CDN（可选）

将静态资源上传到CDN，加快访问速度

---

## ✅ 部署检查清单

- [ ] 宝塔面板已安装
- [ ] Nginx已安装并配置
- [ ] PostgreSQL/MySQL已安装
- [ ] 数据库已创建
- [ ] 项目文件已上传
- [ ] Python依赖已安装
- [ ] 后端环境变量已配置
- [ ] 数据库迁移已完成
- [ ] 前端已构建
- [ ] Nginx反向代理已配置
- [ ] Supervisor守护进程已配置
- [ ] 后端服务已启动
- [ ] 防火墙端口已开放
- [ ] 可以通过浏览器访问
- [ ] API接口正常工作
- [ ] 双模式功能正常

---

## 📞 技术支持

如遇到问题：

1. 查看宝塔面板日志
2. 查看Supervisor日志
3. 查看Nginx错误日志
4. 检查数据库连接
5. 验证环境变量配置

---

**部署完成后访问**: http://101.33.211.98

祝部署顺利！🚀
