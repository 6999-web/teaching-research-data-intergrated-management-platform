# 考评小组端问题诊断和修复

## 问题描述
1. ✓ 手动评分页面的时间显示不是真实时间 - **已修复**
2. ⚠️ 考评小组端无法接收教研室端上传的考评表 - **需要诊断**

## 问题1：时间显示格式修复 ✓

### 修改内容
文件：`frontend/src/views/ManualScoring.vue`

```typescript
// 修改前
const formatDate = (dateStr: string): string => {
  const date = new Date(dateStr)
  return date.toLocaleString('zh-CN', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit'
  })
}

// 修改后
const formatDate = (dateStr: string): string => {
  if (!dateStr) return '-'
  const date = new Date(dateStr)
  const year = date.getFullYear()
  const month = String(date.getMonth() + 1).padStart(2, '0')
  const day = String(date.getDate()).padStart(2, '0')
  const hours = String(date.getHours()).padStart(2, '0')
  const minutes = String(date.getMinutes()).padStart(2, '0')
  const seconds = String(date.getSeconds()).padStart(2, '0')
  
  return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`
}
```

### 效果
- 修改前：`2026/2/28 下午2:30`
- 修改后：`2026-02-28 14:30:45`

## 问题2：考评小组端无法接收考评表

### 可能的原因

#### 原因1：教研室没有提交考评表
**检查方法**：
1. 使用教研室账号登录
2. 填写并提交自评表
3. 确认提交成功提示

**教研室账号**：
- 用户名：`director1`
- 密码：`password123`

#### 原因2：考评表状态不正确
**后端逻辑**：
```python
# backend/app/api/v1/endpoints/scoring.py
# 默认返回以下状态的考评表：
- "submitted"  # 已提交
- "ai_scored"  # AI已评分
- "manually_scored"  # 已手动评分
- "ready_for_final"  # 待最终确定
```

**检查方法**：
1. 查看数据库中的考评表状态
2. 确认状态是否在上述列表中

#### 原因3：考评小组账号权限不足
**检查方法**：
1. 确认使用的是考评小组账号
2. 确认账号角色是 `evaluation_team` 或 `evaluation_office`

**考评小组账号**：
- 用户名：`evaluator1`
- 密码：`password123`
- 角色：`evaluation_team`

### 诊断步骤

#### 步骤1：检查教研室是否提交了考评表

1. **登录教研室账号**
   ```
   访问：http://localhost:3000
   用户名：director1
   密码：password123
   ```

2. **填写并提交自评表**
   - 点击"填写自评表"
   - 填写必填项
   - 点击"提交表单"
   - 确认看到"提交成功"提示

3. **记录提交信息**
   - 教研室名称
   - 提交时间
   - 考评年度

#### 步骤2：检查考评小组端是否能看到

1. **登录考评小组账号**
   ```
   访问：http://localhost:3000
   用户名：evaluator1
   密码：password123
   选择角色：评教小组
   ```

2. **进入手动评分页面**
   - 点击左侧菜单"手动评分"
   - 查看待评分列表

3. **验证结果**
   - [ ] 能看到刚才提交的考评表
   - [ ] 教研室名称正确
   - [ ] 提交时间正确（格式：YYYY-MM-DD HH:mm:ss）
   - [ ] 状态显示为"已提交"或"AI已评分"

#### 步骤3：检查浏览器控制台

1. **打开开发者工具**
   - 按 F12
   - 切换到 Console 标签

2. **查看错误信息**
   - 是否有红色错误
   - 是否有 API 请求失败

3. **查看网络请求**
   - 切换到 Network 标签
   - 刷新页面
   - 查看 `/api/scoring/evaluations-for-scoring` 请求
   - 检查响应状态码和数据

#### 步骤4：检查后端日志

1. **查看后端进程输出**
   ```powershell
   # 在 Kiro 中查看后端进程输出
   ```

2. **查找相关日志**
   - 登录请求
   - 获取评分列表请求
   - 是否有错误信息

### 快速修复方案

#### 方案1：确保教研室提交流程正确

**教研室端操作**：
1. 登录教研室账号
2. 填写自评表（至少填写必填项）
3. 点击"提交表单"按钮
4. 等待提交成功提示
5. 确认状态变为"已提交"

#### 方案2：检查后端API配置

**检查文件**：`backend/app/api/v1/endpoints/scoring.py`

```python
# 确认默认返回的状态包括 "submitted"
query = query.filter(SelfEvaluation.status.in_([
    "submitted",      # 已提交
    "ai_scored",      # AI已评分
    "manually_scored", # 已手动评分
    "ready_for_final"  # 待最终确定
]))
```

#### 方案3：清除浏览器缓存

1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 点击"清除数据"
4. 刷新页面（Ctrl + F5）

### 测试清单

#### 教研室端测试
- [ ] 能够登录教研室账号
- [ ] 能够填写自评表
- [ ] 能够提交自评表
- [ ] 提交后显示成功提示
- [ ] 状态变为"已提交"

#### 考评小组端测试
- [ ] 能够登录考评小组账号
- [ ] 能够进入手动评分页面
- [ ] 能够看到待评分列表
- [ ] 列表中显示教研室提交的考评表
- [ ] 时间显示格式正确（YYYY-MM-DD HH:mm:ss）
- [ ] 能够点击"开始评分"
- [ ] 能够查看自评表内容
- [ ] 能够查看附件
- [ ] 能够输入评分

### 常见问题

#### Q1：列表为空，没有待评分的考评表
**可能原因**：
1. 教研室还没有提交考评表
2. 考评表状态不是 submitted/ai_scored/manually_scored/ready_for_final
3. 数据库中没有数据

**解决方法**：
1. 先用教研室账号提交一个考评表
2. 检查数据库中的考评表状态
3. 确认后端API返回的状态列表

#### Q2：时间显示格式不对
**解决方法**：
1. 清除浏览器缓存
2. 强制刷新页面（Ctrl + F5）
3. 确认前端代码已更新

#### Q3：无法登录考评小组账号
**解决方法**：
1. 确认用户名和密码正确
2. 确认选择了正确的角色
3. 检查后端服务是否运行
4. 查看浏览器控制台错误信息

### 数据库检查

如果需要直接检查数据库：

```sql
-- 查看所有自评表及其状态
SELECT 
    id,
    teaching_office_id,
    evaluation_year,
    status,
    submitted_at,
    created_at
FROM self_evaluations
ORDER BY submitted_at DESC;

-- 查看教研室信息
SELECT id, name, code FROM teaching_offices;

-- 查看用户信息
SELECT username, name, role FROM users WHERE role IN ('evaluation_team', 'evaluation_office');
```

## 修复完成标记

### 已完成
- [x] 修复时间显示格式
- [x] 创建诊断文档
- [x] 创建测试清单

### 待完成
- [ ] 确认教研室提交流程
- [ ] 确认考评小组能看到列表
- [ ] 完成端到端测试

## 下一步行动

1. **立即测试**：按照诊断步骤进行测试
2. **记录结果**：记录每个步骤的测试结果
3. **反馈问题**：如果仍有问题，提供详细的错误信息

## 联系支持

如果问题仍然存在，请提供以下信息：
1. 浏览器控制台截图
2. 网络请求截图
3. 后端日志
4. 具体的错误信息
